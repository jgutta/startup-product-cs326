import React from 'react';
import MainContent from '../maincontent';

export default class CreateThread extends React.Component {
  render() {
    return (
      <MainContent title="Create Thread">Create thread</MainContent>
    )
  }
}
