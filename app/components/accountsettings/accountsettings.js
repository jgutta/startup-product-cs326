import React from 'react';
import MainContent from '../maincontent';

export default class AccountSettings extends React.Component {
  render() {
    return (
      <MainContent title="Account Settings">Account settings</MainContent>
    )
  }
}
